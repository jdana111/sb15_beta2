import { Link } from "react-router-dom";

const SelectProgram = () => (
  <div>
    <h1>SelectProgram</h1>
    <nav>
      <Link to="/">Program</Link>
    </nav>
  </div>
);

export default SelectProgram;
